{
  "name": "wakaru",
  "version": "1.0.0",
  "description": "WhatsApp bot with an agentic AI — download audio/video, make stickers, run commands. Built on Baileys + TypeScript.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun src/index.ts",
    "start:pairing": "bun src/index.ts --use-pairing-code",
    "start:reply": "bun src/index.ts --do-reply",
    "start:node": "node src/index.ts",
    "build:sticker": "cd native/sticker && cargo build --release && mkdir -p ../../bin && cp target/release/wakaru-sticker.exe ../../bin/sticker.exe || cp target/release/wakaru-sticker ../../bin/sticker",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "baileys": "^7.0.0-rc14",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/qrcode-terminal": "^0.12.2",
    "typescript": "^7.0.2"
  }
}
