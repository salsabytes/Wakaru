[package]
name = "wakaru-audio"
version = "0.1.0"
edition = "2021"

[dependencies]
rusty_mp3 = "0.6"
symphonia = { version = "0.5.4", default-features = false, features = ["aac", "isomp4"] }

[profile.release]
strip = true
